<h1>Admin#index</h1>
<p>Find me in app/views/admin/index.html.erb</p>
<ul>
<% Dir.foreach("public/plain/") do |x| %>
  <li>
    <%= link_to 'Goo to' + "#{x}", '/plain/' + "#{x}", { target: '_blank' } %>
  </li>
<%end%>